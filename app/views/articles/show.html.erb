<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= @article.title %></title>

    <!-- Bootstrap -->

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="col-lg-12" style="background-color: #FFFFFF;">
      <div class="row">
          <div class="col-lg-4">
            <div class="text-center">
              <br>
              <%= image_tag @article.feature.url(:large), class: "img img-responsive img-thumbnail", size: "315" %>
            </div>
            <div class="text-center text-capitalize">
            	<h2><%= @article.title %>
              <br>
              <% if User.find(@article.user_id).fname != nil %>
                <small>by: <%= User.find(@article.user_id).fname %> <%= User.find(@article.user_id).lname %></small></h2>
                <% if user_signed_in? %>
                  <% if current_user.id == @article.user_id %>
                    <div class="btn-group">
                      <a type="button" class="btn btn-default btn-sm" href="<%= edit_article_path %>">Edit</a> <%= link_to 'Delete', article_path(@article), :class => "btn btn-sm btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
                    </div>
                  <% end %>
                <% end %>
              <% end %>
            </div>
            <br>
          </div>
          <div class="col-lg-8">
            <br>
          	<p class="lead">
          	  <%= @article.text %>
          	</p>
          </div>
        <br>
      </div>
    </div>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
  </body>
</html>